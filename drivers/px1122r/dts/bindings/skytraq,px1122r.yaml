description: SkyTraq PX1122R RTK GNSS module

compatible: "skytraq,px1122r"

include: uart-device.yaml

properties:
  onepps-gpios:
    type: phandle-array
    required: false
    description: |
      (Optional) One pulse-per-second output.